{
  "name": "quality-gate",
  "version": "0.1.0",
  "private": true,
  "description": "Portable quality gate consumed by any repo via GitHub reusable workflows.",
  "bin": {
    "gate": "cli/gate.js"
  },
  "scripts": {
    "test": "node --test"
  },
  "license": "MIT",
  "devDependencies": {
    "@ast-grep/cli": "0.39.5"
  }
}
